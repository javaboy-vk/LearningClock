# VS Code Support

The repository includes VS Code configuration:

```text
.vscode\launch.json
.vscode\tasks.json
.vscode\settings.json
LearningClock.code-workspace
```

Validate the JSON-based configuration:

```cmd
scripts\dev.cmd validate-config
```
